  export interface TsmResult {
    vertices: number[];    // an array with the route you are looking for (with the vertex traverse order). Instead of int* you can use std::vector<int>
    distance: number;  // the length of this route
  }

export class GraphAlgorithms {
  public static solveTravelingSalesmanProblem(graph: number[][]) {

  }
}
