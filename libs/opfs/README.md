# opfs

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build opfs` to build the library.

## Running unit tests

Run `nx test opfs` to execute the unit tests via [Jest](https://jestjs.io).
